[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['cd backend && npm install']

[phases.build]
cmds = ['cd backend && npm run build']

[start]
cmd = 'cd backend && node dist/src/index.js'
